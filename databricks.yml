bundle:
  name: "lpt-event"

sync:
  include:
    - .build

artifacts:
  default:
    build: uv run apx build

resources:
  apps:
    lpt-event-app:
      name: "lpt-event"
      description: "LPT Event Management Application"
      source_code_path: ./.build




targets:
  dev:
    mode: development
    default: true